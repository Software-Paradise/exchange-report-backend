const vars = require('./vars.config')
const db = require('./db.config')
const constants = require('./constants.config')
const htmlConfig = require('./html.config')

module.exports = { vars, db, constants, htmlConfig}
